{
  "name": "tww3-tournament-platform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e --filter=@rizzotto/e2e",
    "db:generate": "pnpm --filter @rizzotto/db exec prisma generate",
    "db:migrate": "pnpm --filter @rizzotto/db prisma migrate dev",
    "db:migrate:deploy": "pnpm --filter @rizzotto/db prisma migrate deploy",
    "db:seed": "pnpm --filter @rizzotto/db run seed",
    "db:studio": "pnpm --filter @rizzotto/db exec prisma studio",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.10.5",
    "eslint": "^10.3.0",
    "prettier": "^3.4.2",
    "turbo": "^2.9.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.59.3"
  }
}
